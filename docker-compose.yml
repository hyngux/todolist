services:
  db:
    image: mysql:8.0
    container_name: todo_mysql
    restart: always
    environment:
      MYSQL_DATABASE: todo_app
      MYSQL_ROOT_PASSWORD: todo_pass
    ports:
      - "3306:3306"